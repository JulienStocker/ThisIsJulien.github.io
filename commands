Check connection to git repo
ssh-add ~/.ssh/thisischris
ssh-add -l
ssh -T git@github.com


git push origin development:gh-pages

gitquick "deleted highschool education"

Huggin face key
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

deviicon working ools
arduino
anroid studio
anaconda
sql
docker
firebase
gazebo
git
github
gitlab
jupyter
linux
matplotlib
numpy
panda
open cv
pytorch
raspberrpi
ros
tenesorflow
tex

Git return to last commit
git restore .

revert to last commit

Prep workspace
git checkout development
git pull origin development
git checkout gh-pages
git pull origin gh-pages
git checkout development

Merge development to gh-pages
git checkout gh-pages
git merge development

Push to gh-pages
`git push origin gh-pages`
git checkout development
